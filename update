if [ $1 = "-c" ]; then
name="$2"
else
echo "error"
fi

rm Packages.bz2
rm Packages
./dpkg-scanpackages -m ./debs > Packages
bzip2 Packages
git add --all
git commit -m "$name"
git push -u origin master